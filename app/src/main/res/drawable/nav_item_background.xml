<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:drawable="@drawable/light_gray_bg" android:state_pressed="true" />
    <!-- Checked state (for selected items) -->
    <item android:drawable="@drawable/light_gray_bg" android:state_checked="true" />
    <!-- Default state -->
    <item android:drawable="@android:color/transparent" />
</selector>


